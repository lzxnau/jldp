#!/usr/bin/bash

cd $JLDPDIR
docker pull $JLDPIMG
docker run -it --rm --name Jupyter -h Jupyter \
       --network dnet --ip 10.10.0.13 \
       -v $JLDPDIR:/app -w /app \
       -e TZ=Australia/Sydney \
       $JLDPIMG img/jl/dock.run
